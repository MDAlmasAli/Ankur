// অঙ্কুর ভাষায় প্রথম প্রোগ্রাম (a first Ankur program: arithmetic + দেখাও output)
শুরু
    পূর্ণ x = 10;
    পূর্ণ y = 20;
    পূর্ণ যোগফল = x + y;
    দেখাও(যোগফল);

    দশমিক পাই = 3.1416;
    দেখাও(পাই * 2);
শেষ
